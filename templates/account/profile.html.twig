{% extends 'base.html.twig' %}

{% block title %}Modifier son profile{% endblock %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-md-3">&nbsp;</div>
            <div class="col-md-6">
                <h1 class="my-5">Modifier son profil</h1>
                <div class="border border-primary py-5 px-5">
                    {{ form_start(form) }}

                    {{ form_widget(form) }}


                    <div class="row mt-5 mb-3">
                        <div class="col text-center">
                            <button type="submit" class="btn btn-primary">Modifier le compte</button>
                        </div>
                    </div>
                    {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>


{% endblock %}
